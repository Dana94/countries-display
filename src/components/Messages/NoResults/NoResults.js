import React from 'react';

const NoResults = props => {
    return (
        <div>
            No results found.
            <br />
            Do you have at least 1 language and currency selected?
        </div>
    )
};

export default NoResults;
